Live Progress Txt

Bugs:
- make the group dropping css look better
- control config not updating the path??
- the robot does not snap to the starting point when pressing R
- the robot timeline is not getting updated on all events, includes telemetry

Todo:
- Refactor Formats, start writing formats into mikGen
- Make the toString JSON be user editable
- Make it so when you paste in text that is a format ToString it will convert it to a segment
- Temporary remove groups from mikGen
- distance segments
- instead of commands add a help button that provides path examples
- Fix group css for distance traveled so it appears

Planned:
- tooltip when hovering over svg buttons
- make it so double clicking the eye turn off the path lines 
- square drag to select add to path config
- google analytics
- add example paths
- add an edit menu that lists undo redo, the keybinds

Deciding:
- radial click animations
- mode in settings to enable driver control so that way you can use drive curves, draws a path on screen

Docs:
- A better way to plan VEX Autonoumous - EthanMik Channel
- Readthedocs.io showing all the keybinds


lemlib notes
- 

rw notes
- heading correction is separate

Refactor Notes:
- Constants are now arrays
- 
