import type { Robot } from "../../Robot";
import type { LemAngularConstants } from "../LemConstants";

export function turnToHeading(robot: Robot, dt: number, angle: number, k: LemAngularConstants): boolean {
    void [robot, dt, angle, k];
    // TODO: implement
    return false;
}
